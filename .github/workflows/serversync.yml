name: Mirror Repo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Mirror
        run: |
          git clone --bare https://github.com/Rajeshcn02/CloudtoServerSync.git
          cd CloudtoServerSync.git
          git remote add github "https://Rajeshcn02:02211a8567c81b3bc7f2edcba8906407b5c2ec97@github.com/Rajeshcn02/Clouddata.git"
          git push --mirror github 
       # env:
       #   GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
